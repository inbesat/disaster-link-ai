// ---------------------------------------------------------------------
// app/public/dashboard/loading.tsx — DISABLED.
//
// The original skeleton loader caused infinite loading screens when
// child components suspended due to hanging API fetches. Replaced with
// a minimal instant-render placeholder that never blocks the UI.
// ---------------------------------------------------------------------

export default function PublicDashboardLoading() {
  return null;
}
