# DRIP PROJECT — FULL CONTEXT HANDOFF

> Last updated: **August 10, 2026** — UI/UX overhaul Phases 1, 2, 9, 10 are complete and wired. Public dashboard Phase 2 (citizen safety) Steps 1–7 landed this session (hero, geo-fence status, action card, weather carousel, family strip, shelters). Save this file after every session so future chats can pick up instantly.

> **Aug 10, 2026 — state re-verified (fresh chat).** All Phase 9/10 + public-dashboard wiring confirmed present (biometric, offline banner, one-handed mode, count-up/clock/sim, DemoController, ShortcutModal, real-role wiring, md breakpoint). Two deltas found and resolved: ① `lib/ai/openrouter.test.ts` was flaky on this machine — real `.env`/shell provider keys leaked into the suite (`vi.unstubAllEnvs()` restores them), breaking 4 tests. Fixed: `clearProviderKeys()` blanks all five provider-key env vars in `beforeEach` (tests are now truly hermetic, and the "throws when unconfigured" test no longer makes a real network probe). `npx vitest run` → **312/312** (verified twice), `tsc --noEmit` clean, `next lint` clean. ② Working tree carries uncommitted landing-page polish: `app/globals.css` (`.animate-marquee` + a `prefers-reduced-motion` block killing all CSS-driven loops), `app/sections/Solution.tsx` (`loading="lazy" decoding="async"`), `components/landing/command/LiveMapStats.tsx` (inline marquee animation → `.animate-marquee` class).

> **Aug 10, 2026 — Phase 4 (Public Map "Where Do I Go?") Steps 1–2 landed (same session).** `lib/map/citizen-view.ts` (+ 6 tests) — pure SSR-safe resolver turning the saved citizen location (GPS fix / manual district / null) into an initial camera view for the panic-proof map; reuses `HAZARD_ZONES` centroids (same source of truth as the SafetyHero geo-fence) — GPS → street zoom 14, manual district → centroid zoom 11, unknown/none → Patna default (`CITIZEN_MAP_DEFAULTS`). `components/public/map/UserLocationDot.tsx` — renders inside the `<Map>` so `useMap()` reaches the live instance (same pattern as gov LocationSelector): blue dot with a Tailwind `animate-ping` pulse ring (`motion-reduce:animate-none`), shown for BOTH gps + manual (pin at the district centroid), plus the **Locate Me FAB** (Crosshair, bottom-right, `bottom-[calc(96px+env(safe-area-inset-bottom))]` so it clears the fixed BottomNav) that `flyTo`s the resolved view (1.8s, `essential: true`); FAB works even with no saved location (flies to Patna). `components/public/map/PublicMap.tsx` — full-viewport `react-map-gl/maplibre` Map (`mapLib={maplibregl}`, same Carto dark-matter style as the gov DisasterMap), ZERO chrome (no zoom controls/measure tools/layer panels/search): only the base layer + marker/FAB + a small area chip (MapPin + `view.label`, bottom-left) + the legal attribution moved top-right via `attributionControl={false}` + custom `<AttributionControl position="top-right" compact />` (default bottom-right would hide behind the nav). `app/public/map/page.tsx` — `h-[100dvh]` full-screen shell: dynamic `PublicMap` (ssr:false, spinner skeleton "Loading your map…"), floating gradient header (back link → dashboard, Map tile, "FIND YOUR WAY TO SAFETY"), BottomNav (Map tab auto-highlights). **Bonus fix:** `app/sections/Solution.tsx` had a pre-existing unclosed `<TiltCard>` (blocked tsc + lint since before this session) — closed; also restored the file after a botched automated edit. Validation: tsc clean, lint clean, prettier clean, **354/354 tests**.

> **Aug 10, 2026 — Phase 3 (Public Alerts & Notifications) Steps 5–7 landed (same session).** `app/public/settings/alerts/page.tsx` — mobile prefs form: Floods/Cyclones/Earthquakes toggles (role=switch, orange when on), severity **segmented control** (Only Critical / Watch + Critical / All Alerts, aria-pressed buttons — no fake tabs), Quiet Hours switch (10 PM – 6 AM) with the "Critical Life-Safety Alerts always bypass Quiet Hours" sub-toggle **permanently locked ON** (disabled switch + Lock icon + "an imminent flood never sleeps"). Persisted via `lib/mock-data/alert-preferences.ts` (model + localStorage `citizen_alert_preferences`, parse merges over defaults, 10 tests) + `hooks/useAlertPreferences.ts` (hydration-safe: defaults on server/first paint, real value post-mount, write-through). **The prefs actually drive the feed** — `filterAlertsByPreferences` (type filter: only "Floods" has alerts to hide today — cyclone/earthquake toggles are honest no-ops until such alerts exist; severity maps to critical / warning+critical / all). `hooks/useTextToSpeech.ts` — window.speechSynthesis wrapper: `speakAlert` (cancels prior, prefers en-IN voice, guarded), `stopSpeaking`, hook form with `speaking` state driven by end/error events + a 60s watchdog (some platforms never fire `end`), 3 safety tests. **Read Aloud** buttons added to `AlertDetailModal` (floating FAB above the share footer; gated on `supported`) and `CriticalAlertOverlay` (top row; speaks countdown-aware text); both stop speech on close/change. `hooks/useOfflineStatus.ts` — thin inversion of the battle-tested `useOnlineStatus` (starts online both sides). Offline cache in `public-alerts.ts` (`drip:cached-alerts` + `-at` keys, `cacheAlerts`/`readCachedAlerts` with a **full-shape `isPublicAlert` type guard** — reviewer catch: a stale cache missing `actions` would've crashed AlertDetailModal) + `formatCachedAt`; the alerts page writes the cache on load, uses it while offline, and shows a **sticky amber banner** riding inside the sticky header ("Offline Mode — Showing cached alerts from {time}"). Settings reachable via a new SlidersHorizontal icon in the alerts header. Also fixed 4 cache-masked pre-existing unused imports in `app/sections/Hero.tsx` (lint surfaced them when the eslint cache invalidated — same pattern as the Aug 9 sweep). Validation: tsc clean, lint clean, prettier clean, **345/345 tests**.

> **Aug 10, 2026 — Phase 3 (Public Alerts & Notifications) Steps 8–10 landed — PHASE 3 COMPLETE (same session).** Step 8: `PublicAlert` gained `isOfficial: boolean` + `upvotes/downvotes` (guard + mocks updated; 3 official + 2 community so judges see both badges — **spec deviation:** the spec asked for an `isOfficial` card prop; it lives on the data model instead since the card already takes the whole alert — documented). AlertCard renders a blue **"✔ Verified by District Authority"** badge (BadgeCheck, sky tint) for official alerts or a gray **"Community Report"** badge (Megaphone) for community ones, plus tiny **up/down vote pills** (ThumbsUp/ThumbsDown + live tallies, toggleable, haptic; shielded from card-tap/swipe by the existing `closest("button")` onTap guard). Step 9: `components/public/SafeStatusToggle.tsx` — floating pill on the alerts page (bottom-right above the BottomNav): idle green-outline "Mark Myself Safe" → 900ms spinner "Sharing…" → solid green "Marked Safe" + haptic + success toast ("Your status has been shared with registered family members."); persisted to `drip:i-am-safe` (hydration-safe read). Step 10: `components/public/AlertDemoTrigger.tsx` — semi-transparent **Dev Tools** button fixed bottom-left (every public page) opening a tiny modal (role=dialog, Esc/backdrop/X, focus save/restore) with **"Simulate Official Alert"** (dispatches `drip:citizen-demo-alert` with a fresh official IMD rain alert — `createSimulatedOfficialAlert()` — type `rain` so prefs can't hide it; the alerts page prepends it, capped at 10) and **"Simulate CRITICAL Overlay"** (dispatches `drip:citizen-critical-alert`). **Architecture change:** the CriticalAlertOverlay moved OUT of the alerts page into a new layout-level `components/public/PublicAlertHost.tsx` (mounted in `app/public/layout.tsx`) — it owns the overlay + trigger and opens on the window event, so the takeover + dev tools work from ANY public page (event pattern mirrors useDemoSimulation's drip:demo-sim:*). The alerts page now dispatches the once-per-session auto-trigger **inside `queueMicrotask`** — reviewer-caught race: on a full-page load the page's mount effects run before the layout host's listener attaches (sibling order), so a synchronous dispatch was silently lost on first visit. Validation: tsc clean, lint clean, prettier clean, **348/348 tests**.

> **Aug 10, 2026 — Phase 3 (Public Alerts & Notifications) Steps 3–4 landed (same session).** `components/public/CriticalAlertOverlay.tsx` — full-screen takeover: fixed `z-[9999]` on `bg-severity-red-600` with ambient glows, `animate-alert-pulse` "EVACUATE NOW" headline, a **live-ticking** mock countdown ("Flood expected in 04:00:00", `role="timer"`, SSR-safe deterministic start, `formatCountdown` helper), two massive CTAs — white "Show Me Where to Go" → `/public/map` and dark "I Need Help / SOS" → `/public/report`; on mount fires **`triggerCriticalHaptic()`** — new `navigator.vibrate([500,200,500])` trigger added to `hooks/useHaptics.ts` (guarded, returned by the hook form too). Modal conventions: `role="dialog"`/`aria-modal`, focus moved in + restored, body scroll-lock, Esc + plain-styled Dismiss button (deliberately NOT IconButton — its ghost-variant colors would fight the white override, gotcha #3). **Conditional logic lives in `app/public/alerts/page.tsx`:** auto-opens once per tab session when a critical alert is in the feed and `sessionStorage["drip:critical-overlay-seen"]` isn't set (dismiss/CTA sets it; client-only effect, hydration-safe). `components/public/AlertDetailModal.tsx` — spring bottom sheet (max-h 92vh, `role="dialog"`, focus save/restore, Esc/backdrop/X, scroll-lock) opened by **tapping an AlertCard**: severity badge + relative time, official message block, per-alert **Recommended Actions checklist** (new `actions: string[]` field on `PublicAlert`, filled for all 5 mocks, test coverage), CSS-only mock **map snippet** (street-grid bg + pulsing MapPin + "View full map" via `next/link`), and a **Share to WhatsApp** `wa.me/?text=` deep link (emerald styling, no emoji in payload). `AlertCard` gained `onOpen` — tap opens details (framer suppresses tap after a real drag; `onTap` guards `closest("button")` so Mark-read never opens the modal), and the title is now a keyboard-reachable button that renders as a plain `<h2>` when `onOpen` is absent. Validation: tsc clean, lint clean, prettier clean, **326/326 tests** (new: 1 actions-coverage test).

> **Aug 10, 2026 — Phase 3 (Public Alerts & Notifications) Steps 1–2 landed.** `/public/alerts` is no longer a 404. New: `lib/mock-data/public-alerts.ts` — typed `PublicAlert` (`type` flood/rain/road, `severity` safe/warning/critical, `scope` my-area/district/state), 5 Bihar/Patna mock alerts (every filter tab non-empty: My Area 2 · District 2 · State 1), pure `filterAlertsByScope()` + `relativeTime()` (timestamps computed relative to load via `minutesAgoIso` so the demo always reads fresh); `lib/mock-data/public-alerts.test.ts` (**13 tests** — shape invariants, filter coverage, relative-time formatting). `components/public/AlertCard.tsx` — touch-friendly card: 4px severity bar drawn as an **absolute strip** (dodges the border-color-applies-to-all-sides gotcha), Lucide type icon in a tinted tile, SeverityBadge chip + relative timestamp (hydration-safe via a mounted flag, like LiveClock), **framer-motion swipe-right >100px → mark-read** (`drag="x"` constrained to 0 so it rubber-bands, `info.offset.x` threshold; on trigger: opacity fade + green Read chip + `triggerLightHaptic()` + roadmap success toast; `MotionConfig reducedMotion="user"`). `app/public/alerts/page.tsx` — client page in the citizen frame: sticky backdrop-blur header (back link, BellRing tile, live count pill), horizontal hidden-scrollbar **filter bar All / My Area / District / State that really filters** (deliberate small extension over the "placeholder list" step so the tabs aren't inert — swap in real geo logic later), empty state, swipe-hint microcopy, safe-area bottom padding, BottomNav (Alerts tab lights up via route match). Validation: tsc clean, lint clean, prettier clean, **325/325 tests**.

## 🎯 What this project is

**Disaster Response Intelligence Platform (DRIP)** — a hackathon disaster-management web app (flood focus, Patna/Bihar demo district) built on a Next.js 14 (App Router) + TypeScript + Tailwind CSS + Supabase + Prisma + MapLibre GL + Python FastAPI/XGBoost ML microservice stack. Dark-first "Emergency Operations Center" design. Lives in the `disaster-response-platform/` folder.

## 🗺️ Roadmap: everything is DONE except the UI/UX overhaul

The project was built across 25 roadmap phases + 8 extra settings-module phases + Phase 25 multilingual support. The entire codebase was audited against the roadmap — all of it is implemented and wired up:

| Area                                                                 | Status                                                                                                                                                                  |
| -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Phases 1–6** (Foundation, Auth, Map, Data Ingestion, ML, Risk Viz) | ✅ Prisma + Supabase + PostGIS/pgvector migrations, `ml_service/` (FastAPI + XGBoost flood model), MapLibre map with layer toggles, time slider, flood risk polygons    |
| **Phases 7–9** (Alerts, Shelters, Evacuation)                        | ✅ `alert-engine.ts`, web push, shelter CRUD + map popups, `routing.ts` (flood-aware), evacuation plans, road closures                                                  |
| **Phases 10–11** (AI Emergency Planner)                              | ✅ 4 tool modules (`lib/ai/tools/`: shelters, flood, resources, evacuation routes), streaming chat UI with tool badges + sources panel, rate limiting, per-event memory |
| **Phases 12–14** (Resources, Allocation, Command Center)             | ✅ Resource inventory, `fleet-allocation.ts` optimizer, command-center dashboard with KPI cards + realtime charts                                                       |
| **Phases 15–17** (RAG, Multi-Agent, Crowdsourced)                    | ✅ pgvector `embeddings.ts` + `vector-search.ts`, LangGraph agent graph, crowdsourced report triage + Groq classifier                                                   |
| **Phases 18–20** (Admin, Field UI, Realtime)                         | ✅ Full admin panel (8 sections), `/field` mobile responder UI, `lib/realtime/` (presence, conflict resolution, transports)                                             |
| **Phases 21–25** (Security, Polish, QA, Deploy)                      | ✅ `lib/security/` (RLS, rate-limit, sanitize, data-isolation), 281 passing tests, `demo-reset.ts`, `DEMO_CHECKLIST.md`, `/api/health`, Vercel cron, deployment config  |
| **Settings module (8 phases)**                                       | ✅ All 8 sections: profile, notifications, map, AI, organization, privacy, contacts, integrations — each with mock stores + tests                                       |
| **Multilingual**                                                     | ✅ 23 languages (English + all 22 scheduled Indian languages), every locale has all 39 keys, `LanguageSelector` in navbar, SMS alert translation by recipient language  |

## 🔧 AI Chat fixes (important history — don't redo this)

Four stacked bugs were diagnosed and fixed in the AI chat:

1. `anthropic/claude-3.5-sonnet` was removed from OpenRouter → HTTP 404.
2. Primary OpenRouter account had zero credits, backup had ~1,500 tokens (not enough for a reply).
3. Chat route used only `openRouterModel` — Groq/Bluesminds fallbacks existed but were never wired into `/api/chat`.
4. AI SDK v7 stops after ONE model step by default (`stopWhen: isStepCount(1)`) — tools were called but the final answer was never generated.

**Fix:** `lib/ai/openrouter.ts` was rewritten as a probe-based provider resolver: health-checks Groq → OpenRouter primary → OpenRouter backup → Bluesminds with realistic probe requests, skips unconfigured keys, caches the winner for 60s, dedupes in-flight probes, uses current model IDs. Added `lib/ai/openrouter.test.ts` (10 tests). Chat now runs on Groq's free tier — works great, zero cost. The resolver auto-switches to Claude if the OpenRouter account ever gets credits.

**Aug 9, 2026 re-diagnosis ("Planner error — all providers probed"):** the planner page's fallback text is a mid-stream failure catch-all, not a config-detection message. A live masked probe of all four keys (secrets never printed): `GROQ_API_KEY` ✅ 200, `OPENROUTER_API_KEY_BACKUP` ✅ 200, `BLUESMINDS_API_KEY` ✅ 200 — only the primary `OPENROUTER_API_KEY` is dead (402, no credits; the resolver skips it by design, same account documented above). So the keys are all present and valid; the in-app failure was almost certainly **stale env in the running dev server** (Next.js loads `.env` at boot — restart `npm run dev` after adding/rotating keys). Also hardened the probe timeout 8s → 15s in `lib/ai/openrouter.ts` (the live diagnostic needed ~13s worst case, so 8s could false-fail a healthy-but-slow provider).

## 🎨 CURRENT WORK: UI/UX Overhaul — "Discord meets FEMA"

Dark-first design system built on roadmap CSS variables, layered over the legacy EOC palette so the ~150 existing components keep working (documented hybrid approach — **do NOT strip the legacy tokens**).

### Phase 1 — Design system foundation: ✅ COMPLETE (Steps 1–10)

**Step 1 — `app/globals.css`.** All roadmap tokens in `:root`: 15 colors (`--bg-primary: #0a0f1a`, `--bg-secondary: #111827`, `--bg-tertiary: #1e293b`, `--accent-*`, `--text-*`, `--border-subtle`, `--border-active`, `--severity-*`), radii (`--radius-sm/md/lg/xl` = 6/10/14/18px), shadows/glows (`--shadow-modal`, `--glow-blue/red/amber`, `--shadow-card`). Light "day ops" palette under `:root:not(.dark)`, MapLibre dark popup overrides, slim scrollbars, alert-pulse animations. `--*-rgb` channel variables for every color (required for the opacity fix below). Hand-written utility classes (`bg-primary`, `bg-secondary`, `bg-tertiary`, `text-muted`, `border-subtle`, `border-active`, `glow-blue-soft` …) — these **cannot** be Tailwind theme colors (a color named `bg` would generate `bg-bg-primary`), so they're plain CSS that re-themes via variables.

**Step 2 — `tailwind.config.ts`.** `darkMode: "class"`; colors from CSS vars: `accent` namespace (primary/danger/warning/success/purple), `severity` namespace, legacy `border`/`background`/`foreground`/`surface`. **ALL custom colors use `rgb(var(--x-rgb) / <alpha-value>)` so opacity modifiers generate** (this fixed the systemic opacity-modifier bug that silently dropped `/10`, `/40` etc. across ~150 components). `borderRadius` → `var(--radius-*)`; `boxShadow` → `card`/`modal`/`glow-blue`.

**Step 3 — `components/ui/SeverityBadge.tsx` (upgraded) + `StatusDot.tsx` (new).** SeverityBadge: `variant` prop (safe/watch/warning/critical), `--severity-*` tints + `--accent-*` border/text, Lucide icons, pulse on warning/critical, `normalizeSeverity()` maps loose strings. StatusDot: 8px circle, `online/offline/busy`, `animate-ping` ring when online, `role="status"`.

**Step 4 — `StatCard.tsx` + `DataRow.tsx` (new).** StatCard: `bg-secondary` + `rounded-md` + `border-subtle`, `text-3xl` bold `tabular-nums` value (Indian grouping → 47,230), trend arrow with `trendClassName` override (up isn't always good in disasters), optional icon tile, hover lift. DataRow: `icon`/`title`/`subtitle?`/`trailingElement?`, optional `onClick` → keyboard-navigable with accent focus ring.

**Step 5 — `Panel.tsx` + `IconButton.tsx` + `BackButton.tsx` (new).** IconButton: icon-only, `label` required, variants `ghost/filled/danger/floating`, sizes `sm/md/lg`. Panel: `bg-secondary` + `rounded-lg` + `shadow-card` + `border-subtle`, header slot + padded body + optional footer. BackButton: `router.back()` with dead-end guard → `/command-center`; wired into all 4 shells (Navbar, AdminSidebar, SettingsSidebar, FieldShell).

**Step 6 — `components/ui/SkeletonLoader.tsx` (new) + globals.css shimmer.** `SkeletonLoader` accepts `width`/`height`/`borderRadius` (numbers → px, strings pass through; props win via inline styles). Continuous shimmer via `--skeleton-base`/`--skeleton-shine` CSS variables (dark = your spec's slate-800/700, light = slate-200/100) + `@keyframes skeleton-shimmer` + `.skeleton-shimmer` (1.6s, `background-size: 200%`) inside `@layer components` (same pattern as `alert-pulse`), `prefers-reduced-motion` static fallback. **Presets:** `SkeletonCard` (mirrors StatCard anatomy: label 14px / icon tile 32px / value 30px / trend 12px) and `SkeletonRow` (mirrors DataRow: icon 36px + title 14px / subtitle 11px + trailing pill). Legacy `Skeleton.tsx` left untouched.

**Step 7 — Toast system: `components/ui/Toast.tsx` + `hooks/useToast.ts` (new).** Wraps **react-hot-toast** (already used by ~70 call sites + 4 mock stores — a parallel system would double the toast stacks). `Toast` severity card rendered via `toast.custom` (owns its whole box): `bg-secondary` + `border-subtle` + `rounded-lg` + `shadow-card`, 3px left accent strip in the severity color, Lucide icon, `IconButton` dismiss X, **5s shrinking progress bar** (`.toast-progress` keyframes, hover-pause is visual-only — the lib timer keeps running), smooth fade/slide-out off `t.visible`. `showToast`/`dismissToast`/`dismissAllToasts` return the toast id. `ToastViewport` replaces `<Toaster>` in `app/layout.tsx` — position flips **bottom-right (desktop) → top-center (mobile)** via a `matchMedia` hook (react-hot-toast's per-toast wrappers are absolutely positioned with inline styles, so CSS overrides can't relocate them); base `toastOptions.style` re-skins all legacy `toast.success/error/loading("…")` call sites. `useToast()` returns stable `{ success, warning, error, info, dismiss, dismissAll }`, each taking `{ title, description?, duration?, id? }`.

**Step 8 — Design Tokens Preview: `app/(app)/styleguide/page.tsx` (new, `"use client"`).** Hidden internal Storybook at `/styleguide` (bare `(app)` route group, no nav links). **Colors:** `ColorSwatch` grids resolve CSS variables at runtime via `getComputedStyle` + a `MutationObserver` on `<html>` class (swatches re-read when the theme flips dark ⇄ day-ops) — all `--bg-*`, `--accent-*` (incl. legacy `--accent`/`--accent-soft`), `--severity-*`, plus all 16 legacy EOC severity scale vars. **Typography:** H1/H2/H3/body/mono examples rendered as non-semantic `<p>` (so they don't pollute the page's heading outline). **Components:** live SeverityBadge (all variants), StatusDot, StatCard (incl. red-trend "up is bad"), DataRow, IconButton (all variants/sizes), SkeletonLoader/SkeletonCard/SkeletonRow, and the Phase 2 sidebar demos. Wrapped in reused `Panel`s with a `BackButton` header.

**Step 9 — Fonts in `app/layout.tsx`.** `Inter` + `JetBrains_Mono` from `next/font/google` expose **`--font-sans`** / **`--font-mono`** (renamed from old `--font-inter`/`--font-jetbrains-mono`; updated the 5 references in `tailwind.config.ts` fontFamily + globals.css body stack), applied to `<body>` with explicit `bg-primary text-primary` + `antialiased`. Self-hosted at build, zero CLS. Browser-verified: body computes to Inter, mono elements to JetBrains Mono.

**Step 10 — Theme provider: `components/providers/ThemeProvider.tsx` (new).** `next-themes` (already installed, `^0.4.6`) with `attribute="class"`, `defaultTheme="dark"`, `enableSystem={false}`, `disableTransitionOnChange`. **Decision recorded (REVERSED Aug 9 — see "Demo-day hardening · Step 6" below):** dark-default enforced, and the `ThemeToggle` was KEPT (Navbar + landing page) so day-ops stays reachable — the app always _starts_ dark. Old `components/ThemeProvider.tsx` deleted (only `layout.tsx` referenced it); provider sits inside `<body>` wrapping LanguageProvider/MapSettingsProvider/children; `<html>` already has `suppressHydrationWarning`. Browser-verified toggle flips dark ⇄ light.

**Phase 1 library now:** SeverityBadge ✅ StatusDot ✅ StatCard ✅ DataRow ✅ Panel ✅ IconButton ✅ BackButton ✅ SkeletonLoader (+SkeletonCard/SkeletonRow) ✅ Toast/useToast/ToastViewport ✅ Styleguide ✅ Fonts ✅ ThemeProvider ✅ (plus pre-existing `Skeleton`, `EmptyState`, `SystemErrorFallback`, `OnboardingTooltip`, `LanguageSelector`, `Translated`).

### Phase 2 — Web sidebar navigation: ✅ COMPLETE (Steps 1–10; state re-verified Aug 9, 2026)

**Step 1 — Sidebar shell: `components/navigation/Sidebar.tsx` + `SidebarHeader.tsx` (new).**

- **Sidebar:** `flex h-screen flex-col border-r border-subtle bg-secondary`, width animates **260px ⇄ 64px** (`w-[260px]`/`w-16` + `transition-all duration-300 motion-reduce:transition-none`). Fixed variant: `fixed inset-y-0 left-0 z-40`. **Collapse toggle** (ChevronLeft/Right) in a bottom footer — reuses `IconButton` (ghost/md), flipping `aria-label`, `aria-expanded` + `aria-controls="app-sidebar"` on the button, `id="app-sidebar"` on the aside. **Uncontrolled** (internal `useState`) or **controlled** via `collapsed` + `onToggle` (for future layout wiring). **`variant="inline"`** renders in-flow (used by the styleguide demo — avoids the fixed-escapes-Panel pitfall). Scrollable `<nav>` middle region `min-h-0 flex-1 overflow-y-auto overflow-x-hidden px-2 py-3` holds sections/items.
- **SidebarHeader:** logo tile (`Activity` icon, `bg-accent-primary/15` + `shadow-glow-blue`), pulsing **StatusDot (online)** badge on the corner, brand "DRIP — Command Center" **only when expanded**, kept mounted with width/opacity transition so collapse animates (logo centers itself in the 64px rail).

**Step 2 — Nav atoms: `components/navigation/sidebar-context.ts` + `SidebarSection.tsx` + `SidebarNavItem.tsx` (new).**

- **`sidebar-context.ts`:** tiny `{ collapsed }` context + `useSidebar()` hook — nav atoms auto-adapt to the sidebar state (internal or controlled) without prop-threading; degrades to "expanded" outside a `<Sidebar>` so atoms work standalone.
- **`SidebarSection.tsx`:** label (`--text-muted`, uppercase, `tracking-wider`, e.g. "OPERATIONS") **rendered only when expanded**; when collapsed a subtle `border-t border-subtle` divider + `mt-3 pt-3` (with `first:mt-0 first:border-t-0 first:pt-0` on the first section) keeps the icon rail scannable.
- **`SidebarNavItem.tsx`:** Next `Link` with `icon` (LucideIcon), `label`, `href`, optional `badgeCount` (99+ formatting, **hidden at 0** via `badgeCount > 0` gate), optional `active`. **Active state** (auto-derived via `usePathname` — `isPathActive`: exact for `/`, segment-aware prefix otherwise — unless `active` passed): `border-l-2 border-accent-primary` + `bg-accent-primary/10` tint + accent icon + `aria-current="page"`. **Hover:** `hover:bg-white/5`. **Collapsed:** label + badge hidden, icon centered, `aria-label` on the link, and a **tooltip** (`role="tooltip"`, `bg-tertiary`/`border-subtle`/`shadow-card`) on hover AND keyboard focus.
- **Wiring:** `Sidebar.tsx` wraps its content in `<SidebarContext.Provider value={{ collapsed: isCollapsed }}>`.
- **Styleguide demo upgraded:** placeholder items replaced with real `SidebarSection` + `SidebarNavItem` usage — "Operations" (Command Center **active**, Map, Resources) + "Alerts" (Active Alerts **badgeCount={12}**, AI Planner, Field Ops) inside the interactive `variant="inline"` demo.

**Step 3 — Composed nav: `components/navigation/DashboardSidebar.tsx` (new) + `lib/config/navigation.ts` (new).** The nav is now **config-driven**: `NAVIGATION_ROUTES` in `lib/config/navigation.ts` is the single source of truth — 10 routes (Dashboard → `/command-center`, Live Map → `/command-center`, Alerts → `/alerts`, Evacuation Routes → `/evacuations`, Shelters → `/shelters`, Resources → `/inventory`, AI Planner → `/ai-planner`, Satellite → `/settings/integrations`, Team → `/directory`, Settings → `/settings/profile`) grouped into sections OPERATIONS / RESOURCES / INTELLIGENCE / TEAM / SETTINGS, each with an `allowedRoles: Role[]` array. `filterRoutesByRole()` filters by role. **Role matrix:** field_responder → Dashboard, Live Map, Alerts, Evacuation Routes, AI Planner, Team · district_admin → + Shelters, Resources, Satellite · super_admin → everything incl. Settings. DashboardSidebar maps over the config filtered by a `userRole` prop (mock default `district_admin` per spec — the server layout can pass the real role later with zero changes), renders SidebarSection + SidebarNavItem per route, skips empty sections, and keeps the Active Alerts pill via `alertsBadgeCount`. Route targets are the REAL pages that exist (Live Map → command-center where the map is embedded; Satellite → settings/integrations which has the Satellite GIS card). `viewer` is intentionally on no route (bounced to /403 anyway). Stylguide demo gained a mock-role selector (field_responder/district_admin/super_admin) proving the filter live; `lib/config/navigation.test.ts` locks the matrix (10 tests).

**Step 4 — Dashboard shell wiring: `DashboardShell.tsx` + `DashboardTopBar.tsx` (new), `app/(dashboard)/layout.tsx` rewired.** `DashboardShell` (client) owns the collapsed state and renders the fixed `DashboardSidebar` (wrapped in a plain `hidden md:block` div — avoids the flex-vs-hidden stylesheet-order trap, gotcha #3) plus a content column whose left margin animates `md:ml-[260px] ⇄ md:ml-16` with the collapse (mobile stays full-width; the top bar's hamburger covers mobile nav). `DashboardTopBar` (client) relocates the old Navbar's right-side cluster into a slim sticky bar — avatar+name (guest icon in demo), SyncStatus (lg), PresenceIndicators, NotificationCenter, PushNotificationToggle (md), Settings link, LanguageSelector, ThemeToggle, NavbarMobileMenu, sign-out/exit-demo form — with identity resolved server-side in the layout and passed as props. The layout now computes a **live Active Alerts badge** via `prisma.alertLog.count({ where: { isAcknowledged: false } })` (try/catch → fallback 2, mirroring the alerts page's mock) and keeps `<AlertTicker />` between the top bar and the page. Navbar.tsx and its child NavbarNav.tsx are no longer imported by any layout (left in place, unused — only those two; NavbarMobileMenu/NavbarAvatar/LanguageSelector/BackButton are still used by DashboardTopBar; ThemeToggle was too, until the demo-day dark lock removed it — see Demo-day hardening · Step 6). Note: the styleguide composed-nav demo no longer forces the `active` state on an item (auto-derives from the route, and /styleguide matches none) — the active accent state is browser-verified live on /command-center instead.

**⚠️ Tooltip architecture (important — the CSS-only approach silently broke):** the nav is a scroll container (`overflow-x-hidden`), so a plain `absolute left-full` tooltip whose containing block (the `relative` Link) sits _inside_ that clipping ancestor gets clipped at the 64px rail edge — the tooltip was completely invisible. **Fix:** the tooltip is now measured via `getBoundingClientRect()` on pointerenter/focus and rendered with `position: fixed` + viewport coordinates (escapes every ancestor overflow clip — verified no transform/filter ancestor anywhere). Also: **two-state pattern** — position persists (`tipPos`), visibility is a separate flag (`tipVisible`), so the fade-out happens in place instead of the tooltip teleporting to `(0,0)` to fade in the corner. Next.js 14 `Link` forwards refs to the `<a>` (confirmed). Guard `showTip` on `collapsed`; `useEffect` clears state when the sidebar expands.

**Step 5 — Quick actions panel: `components/navigation/QuickActions.tsx` (new), mounted in `Sidebar.tsx` (not part of the original roadmap — added for demo-day emergency).** Compact in-shell panel sitting between the scrollable `<nav>` region and the collapse footer: `mx-2 mb-1 rounded-lg border border-subtle bg-tertiary p-2`, "QUICK ACTIONS" micro-label, and two high-contrast one-click buttons — **Send Alert** (`Siren` icon, `bg-accent-danger/15` tint + accent-danger text, `toast.warning("Emergency alert queued")`) and **Run AI Plan** (`Zap` icon, `bg-accent-primary/15` + accent-primary text, `toast.info("Planner started")`). Icons are Lucide — **no emoji** (design system rule). **Self-hides at the 64px rail** via `useSidebar().collapsed`, so the mount in `Sidebar.tsx` is unconditional. Note: `components/ai/QuickActions.tsx` (AI-planner prompt chips) and `components/field/QuickActionsNav.tsx` still exist — no name collision; the new one lives in the `navigation/` namespace.

**Step 6 — Global keyboard shortcuts: `hooks/useHotkeys.ts` (new) + `shortcut` prop on `SidebarNavItem` + wiring in `DashboardSidebar` (demo-day power-user feature).** `useHotkeys(shortcuts)` binds a single `window` keydown listener over a `Record<string, HotkeyAction>` — key = `+`-joined combo (`mod`/`cmd`/`ctrl`/`meta`/`alt`/`shift` + a key), value = a **route string** (navigated via `useRouter().push`) or a callback. `mod` = Cmd on macOS / Ctrl elsewhere (strict modifier matching via `navigator.platform`); matches `preventDefault`; typing in inputs/textareas/contenteditable never fires (guards `isEditableTarget`). SidebarNavItem gained an optional `shortcut?` display prop — rendered as a muted roadmap-themed `<kbd>` (`border-subtle bg-tertiary font-mono text-muted`) to the right of the label, expanded-only (follows gotcha #10; the legacy kbd in SettingsSearchBar used literal `#2c3f6d`). **Wiring:** `DashboardSidebar` binds `mod+1..mod+6` → command-center/alerts/evacuations/inventory/ai-planner/directory and passes matching ⌘-labels to the row; a `Set` dedupes the shortcut hint for the shared `/command-center` href (Dashboard + Live Map). Manual demo shows hints on the first item per href only.

**Step 7 — Responsive mobile drawer + desktop state persistence (finalized `Sidebar.tsx`; wired through `DashboardSidebar`, `DashboardShell`, `DashboardTopBar`).** On desktop (`lg+`, `useIsDesktop()` via `matchMedia("(min-width: 1024px)")`) the sidebar collapses 260⇄64 as before; on mobile it becomes an **off-screen drawer** — base `-translate-x-full lg:translate-x-0`, `translate-x-0` when `isOpenMobile`, with a `bg-black/50` backdrop (z-30 under the z-40 aside), click-outside + Escape to close, body scroll-lock while open. **Context collapse is forced to expanded on mobile** (`effectiveCollapsed = isDesktop ? isCollapsed : false`) so the drawer always shows labels; the collapse footer is `hidden lg:flex`; `COLLAPSED_WIDTH` is now `w-[260px] lg:w-16` so the rail only shrinks on desktop. **Persistence:** `readSidebarCollapsed()`/`writeSidebarCollapsed()` exported from `Sidebar.tsx` (key `drip:sidebar-collapsed`, `typeof window` + try/catch guards); uncontrolled mode lazy-inits from storage (no flash on mount), an effect writes every change for both controlled + uncontrolled, fixed variant only — inline styleguide demos never touch storage. **Shell wiring:** the `hidden md:block` wrapper is gone (Sidebar hides itself via translate); `DashboardShell` seeds `collapsed` from `readSidebarCollapsed()`, owns `mobileOpen`, and moves the content margin to `lg:`; `DashboardTopBar` gained an `onOpenMobile` hamburger (`Menu` icon, `lg:hidden`) that opens the drawer. This **replaces the `NavbarMobileMenu` usage** in the dashboard top bar (the old blurred dropdown); `NavbarMobileMenu`, `NavbarNav` and `Navbar.tsx` became orphaned — all three were DELETED Aug 9, 2026 (see Codebase audit pass → follow-up). Breakpoint for the drawer was `lg` (1024px) per this step — **MOVED to `md` (768px) Aug 9, 2026** to match the architecture doc (see Codebase audit pass → follow-up).

### Phase 9 — Mobile & field polish: ✅ COMPLETE (Steps 8–10; wired Aug 9, 2026)

**Step 8 — Biometric Auth Prompt: `components/auth/BiometricPrompt.tsx` (verified existing) + wired into login.** Modal simulating Face ID / Fingerprint enrollment: Lucide Fingerprint in an accent ring (pulsing ping while "scanning"), ~900ms mock scan → ShieldCheck success flash, **Enable / No Thanks** buttons, framer-motion spring sheet (`z-[70]`). Preference persisted to localStorage (`drip_biometric_enabled_v1`, SSR-safe `readBiometricPref`/`writeBiometricPref`, `supportsBiometricAuth()` mock gate, `disableBiometric()`). **Wiring (new):** `app/(auth)/login/LoginForm.tsx` — after a successful email sign-in, `setAuthed(true)`; if `readBiometricPref()` is already stored the effect navigates straight on, otherwise the prompt mounts and its `onClose` performs the redirect. (Google OAuth + guest mode skip the prompt.)

**Step 9 — Offline Mode Indicators: `hooks/useOnlineStatus.ts` + `components/ui/OfflineBanner.tsx` (new) + mounted.** Hook: single `online`/`offline` listener pair, starts `true` on both server & first client paint (no hydration mismatch), corrected on mount. Banner: amber strip (`bg-severity-warning` + `border-accent-warning/40`, re-themes in day-ops) dropping from the viewport top (`fixed top-0 z-[70]`, framer-motion spring), Lucide WifiOff (no ⚠️ emoji — design-system rule; deviation documented), "Offline Mode — Data may be stale.", mock "Last synced: 2 mins ago" timestamp, **Retry Connection** ghost button (re-probes `navigator.onLine` → roadmap toast success/warning), `role="status"`, `MotionConfig reducedMotion="user"`, unmount-safe probe timer. **Mounted (new)** in `app/(dashboard)/layout.tsx` (dashboard scope — the field app mounts its own `components/field/OfflineBanner.tsx`).

**Step 10 — One-Handed Accessibility Mode: `BottomNav.tsx` + `DashboardShell.tsx` (updated).** Rapid double-tap (≤300ms) on the bottom nav's **background only** (tabs/FAB excluded via `closest("button, a, [role='button']")`) toggles iOS-style Reachability: the shell's content column gets `translate-y-[25vh]` (animates on the existing `transition-all duration-300`, `motion-reduce` respected) so top-of-page controls land in thumb range. **Key gotcha:** swipe-cancellation must NOT live in `onTouchStartOrOnMouseDown` — react-swipeable fires that on every tap's `touchstart`, which wiped the pending tap before the second one ever arrived (feature was silently dead on touch devices); the cancel now lives in the `onSwiped*` callbacks. Extras: haptic buzz, hint toast on enable, bar top edge tints `border-t-accent-primary` while active, a **Restore chip** above the bar so the mode isn't gesture-only (keyboard/AT reachable), and a `matchMedia("(min-width: 1024px)")` auto-exit (the BottomNav hides on desktop, so the mode can't get stuck).

### Phase 10 — Demo-day polish & impressions: ✅ COMPLETE (Steps 1–4; wired Aug 9, 2026)

**Step 1 — Animated Counting Numbers: `components/ui/CountUpNumber.tsx` (new) + `HeroKPIs.tsx` (updated).** Client component: `value` + `duration` (default 1500ms) + `locale` (default `en-IN` → 47,230), a single rAF loop with ease-out-cubic, exact final value, restart on value change, `prefers-reduced-motion` snap, SSR-safe (starts at `from` on both sides). **New `from?` prop** (default 0) so live updates animate prev→new instead of re-counting from zero — used by the Step-3 simulation. HeroKPIs wraps People at Risk (47,230), Shelters (`18`/24), Resources (`156 units`) via StatCard's `valueNode`; the risk card stays a text badge. Legacy `KPICards.tsx` values (People at Risk 48,210 / Shelters 132 / Resources 1,847 / Responders 863) are ALSO wrapped in `CountUpNumber` (done Aug 9), and the People-at-Risk card now **subscribes to the demo-sim bump event too** (`from`/`value` state, same pattern as HeroKPIs) — the drifting numbers are visible on the guest `/command-center` surface, which is where judges actually land (HeroKPIs alone lives on the admin-only `/dashboard` route; guests are bounced to /403 there).

**Step 2 — Ticking Live Clock: `components/dashboard/LiveClock.tsx` (new, extracted from DashboardHeader) + mounted.** IST via `Intl.DateTimeFormat` (`Asia/Kolkata`, `hourCycle: "h12"`), `setInterval` 1s, "HH:MM:SS AM/PM IST" with **seconds muted** (`--text-muted`), SSR-safe `--:--:-- -- IST` placeholder, `role="timer"` (no live region — never announced), re-syncs on `visibilitychange` (hidden-tab interval throttling), slate-literal styling matching the non-retheming hero header slab. Mounted in `DashboardHeader`'s center cluster next to the pulsing green dot (inline clock code removed). **Aug 9 evening:** ALSO mounted in `DashboardTopBar` (`hidden lg:flex`, centered next to a pulsing green dot) via a new `timeClassName` prop (`text-primary`) — the top bar re-themes in day-ops, so the hardcoded `text-slate-100` would be unreadable there; only one text-* class renders (no cascade conflict). The clock now ticks on EVERY dashboard page incl. the guest command center; `/dashboard` renders both clocks (hero header + top bar) showing the same IST time — harmless.

**Step 3 — Simulated Live Data Injection: `hooks/useDemoSimulation.ts` (new) + wired.** While localStorage `demo_sim_active` === "true", an 8s interval randomly (55/45) dispatches typed window events: `drip:demo-sim:people-at-risk` (`{ delta }`, 5–20) or `drip:demo-sim:activity` (`{ event }` — exact `RealtimeEvent` shape, `RESOURCE_MOVE`). Flag re-checked on mount, `storage` (cross-tab), `drip:demo-sim:changed` (same-tab, fired by `setDemoSimActive()`), visibilitychange, and inside each tick. **Wiring (new):** `useDemoSimulation()` mounted in `DashboardShell`; `HeroKPIs` subscribes → `setPeopleAtRisk((k) => ({ from: k.value, value: k.value + delta }))` (subtle count-up via the `from` prop); `LiveActivityFeed` subscribes → prepends the log above the realtime feed (`[...simEvents, ...liveEvents]`); **`KPICards` (guest command center) subscribes the same way** so the drift is visible on the demo surface. Enable at pitch time by setting the flag (or via a future toggle).

**Step 4 — Secret Demo Mode Controller: `components/demo/DemoController.tsx` (new) + mounted.** Renders nothing unless the URL carries `?demo=1` (read client-side, starts `false` both sides, re-read on every route change via `usePathname` — App Router doesn't remount the root layout). When active: semi-transparent bar fixed at the bottom (`bg-black/70`, `z-[9999]`), subtle tracked-out "DEMO MODE ACTIVE" watermark, and three emoji-labeled pills (💥 Trigger Critical Flood / ✅ Complete Evacuation / ⚠️ Deplete Resources — emoji kept verbatim per spec for this hidden dev tool, deviation documented) driven by a `SCENARIOS` config table; each fires the roadmap toast (`error`/`success`/`warning`) + `console.log` to prove interception — the table is the single edit point for real state injection later. **Mounted (new)** in `app/layout.tsx`.

### Phase 11 — Power-user polish · Step 5: Keyboard Shortcuts Modal: ✅ COMPLETE (wired Aug 9, 2026)

**`components/ui/ShortcutModal.tsx` (new) + mounted in `app/layout.tsx`.** Pressing **`?` (Shift + /)** anywhere opens a dark shortcuts reference — roadmap tokens (`bg-secondary`/`border-subtle`/`bg-tertiary` + `shadow-card`), framer-motion spring (`z-[70]`, same tier as BiometricPrompt), `MotionConfig reducedMotion="user"`, `role="dialog"` + `aria-modal`, Esc / backdrop-click / X `IconButton` to close, focus moved into the panel on open. The grid lists the app's REAL hotkeys as styled `<kbd>` chips (same look as the sidebar's shortcut hints — `border-subtle bg-tertiary font-mono`): **⌘1 Dashboard · ⌘2 Alerts · ⌘3 Evacuations · ⌘4 Resources · ⌘5 AI Planner · ⌘6 Team · ⌘K Search settings · ? panel · Esc close** (AI Planner row shows both `⌘5` and `⌘/`). The modifier glyph is platform-corrected post-mount (`⌘` macOS / `Ctrl` else — SSR-safe, no hydration mismatch). The component ALSO wires the new **`⌘/` → `/ai-planner`** global alias (strict modifier match; `⌘⇧/` still opens the panel) and reuses `isEditableTarget` (now exported from `hooks/useHotkeys.ts`) so typing `?` in a form field never opens the modal. Pro-tool touches: the dialog **closes on route change** (pressing `⌘/` while it's open navigates AND dismisses it — no dialog floating over the next page) and **focus is saved on open / restored on close**.

**Spec deviation (deliberate):** the step spec said `⌘2 = Map`, but Phase 2's real binding is `⌘2 = Alerts` (wired in `DashboardSidebar`'s `useHotkeys`); the modal shows the truth rather than a shortcut that lies to judges. Re-mapping ⌘2 → Map later would need to update `NAV_SHORTCUTS`/`NAV_SHORTCUT_LABELS` in `DashboardSidebar.tsx` and this grid.

### Demo-day hardening · Step 6 — Dark mode LOCKED: ✅ COMPLETE (Aug 9, 2026)

**`components/providers/ThemeProvider.tsx`** now sets `forcedTheme="dark"` (plus `defaultTheme="dark"`, `enableSystem={false}`, `disableTransitionOnChange`): next-themes always writes the `dark` class, ignores storage/OS, and `useTheme().setTheme()` becomes a no-op — a projector demo can never white-flash. **`components/ThemeToggle.tsx` was REMOVED** from the landing page (`app/page.tsx`), the orphaned `Navbar.tsx`, and `DashboardTopBar.tsx` (each keeps a comment pointing here), then **deleted** (restorable from git history). The day-ops palette under `:root:not(.dark)` in globals.css is untouched (hybrid approach) but unreachable while `forcedTheme` is set; the styleguide copy now says so instead of inviting a toggle flip. **Re-enable post-hackathon:** drop `forcedTheme="dark"` from the provider and restore the toggle component.

### Demo-day hardening · Step 7 — Iconography & spacing: ✅ COMPLETE (Aug 9, 2026)

**`components/ui/Icon.tsx` (new)** — the single standardized entry point for lucide-react icons (no mixing icon libraries). `name` is a string TYPED against an explicit `ICON_REGISTRY` (covers every icon imported anywhere in the app today — typos are compile errors, autocomplete works; adding an icon = 1 import + 1 registry line). Named imports only, so it tree-shakes — no runtime string→module lookup, no dynamic-import flash. `size` accepts px or the semantic presets `"nav"` (20, default) / `"action"` (24) / `"inline"` (16); `className` passes straight to the SVG. **Every icon renders `strokeWidth={1.5}`** (lucide's default is 2) so stroke weight is uniform at every size; the stroke is relative, so proportions stay identical from a 16px chip to a 24px action. Showcased on /styleguide (new demo well: three sizes + a colored example). Existing ~150 components still import lucide directly — migration is incremental: `import { Icon } from "@/components/ui/Icon"; <Icon name="Bell" size="inline" />`.

### Demo-day hardening · Step 8 — Data density & whitespace polish: ✅ COMPLETE (Aug 9, 2026)

**`components/ui/DataRow.tsx`** tightened: padding `px-3 py-2.5` → **`py-3 px-4`**; title locked to `text-sm font-medium text-primary` (was already correct); subtitle STRICTLY `text-xs text-muted` (was `text-[11px]`, mono readout kept — `font-mono text-xs text-muted`); hover stays the gotcha-#2-correct `hover:bg-[var(--bg-tertiary)]` (hand-written class, can't take variants). No API change — all ~existing call sites render the tighter rows automatically.

### Demo-day hardening · Step 9 — Accessibility (a11y) fast pass: ✅ COMPLETE (Aug 9, 2026)

**`components/ui/IconButton.tsx`** audited for automated a11y checks: ① accessible name is compile-enforced — the `label` prop is REQUIRED and renders as `aria-label` + `title`, so an icon-only button without a name cannot be built; ② keyboard focus now `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-primary focus-visible:ring-offset-2 focus-visible:ring-offset-[var(--bg-primary)]` (was `/70` opacity; ring-offset must be an arbitrary var — `bg-primary` is hand-written, `focus:ring-offset-bg-primary` would silently not generate, gotcha #1/#2); `focus-visible:` intentionally preferred over `focus:` so mouse clicks don't flash a ring; ③ `role="button"`/`tabIndex={0}` deliberately NOT added — native `<button>` already exposes both (redundancy is a lint smell). No API change.

### Codebase audit pass (Aug 9, 2026) — lint + overlay bugs fixed

First-ever `next lint` run exposed 16 errors + 1 warning, all fixed: ① `react/no-children-prop` (3×) — `SidebarNavItem`'s accordion prop was named `children`; **renamed to `subRoutes`** everywhere (`SidebarNavItem.tsx`, `DashboardSidebar.tsx`, `lib/config/navigation.ts` `NavRoute.subRoutes` + Settings entry, styleguide demos). ② `react/no-unescaped-entities` (13×) in the styleguide copy — straight quotes/apostrophes in JSX text escaped (`&quot;`/`&apos;`/expressions). ③ `react-hooks/exhaustive-deps` — `LoginForm.finishLogin` was an inline closure the effect captured; now `useCallback([router, next])` + added to deps. ④ Documented Phase-9/10 overlay bug fixed: root-layout `SimulationToggle` wrapper is now `fixed bottom-[84px] right-4 z-50 lg:bottom-4`, so below `lg` it clears the 72px mobile BottomNav gutter instead of covering it. **`npx next lint` → "✔ No ESLint warnings or errors"**. `console.log` audit: only dev scripts, a comment, the deliberate DemoController intercept, and server-side twilio logs — no client debug leftovers. **Follow-up (same day) — both flagged items are now FIXED:** ① breakpoints `lg` → `md` (768px) per the architecture doc — the mobile drawer / pinned-sidebar switch, bottom nav, top-bar hamburger and LiveClock now key on `md` (tablets 768–1023 get the pinned sidebar + NO bottom nav; phones <768 keep the drawer + 72px bottom nav). Touched: `Sidebar.tsx` (`useIsDesktop` → `useIsTabletUp`, matchMedia 768, `COLLAPSED_WIDTH`/backdrop/translate/footer), `DashboardShell.tsx` (margins `md:ml-*`, `md:pb-0`, one-handed auto-exit at 768), `DashboardTopBar.tsx`, `BottomNav.tsx` (`md:hidden`), plus the aligned companions `ContextActionBar.tsx`, `ContextFAB.tsx` (`md:bottom-6`), the root-layout `SimulationToggle` (**bottom-LEFT** now — `bottom-[calc(72px+env(safe-area-inset-bottom)+12px)] left-4 z-50 md:bottom-4 md:left-4`) and `EmergencyContactCard` (**`hidden md:flex`** — it was the real coverer of the BottomNav's right tabs on phones; the SOS EmergencyFAB covers phones). Settings/admin/ai-planner layouts keep their own `lg` patterns (separate surfaces). `scripts/check-phase9-10.mjs` one-handed check moved to a 600px viewport + hydration threshold 768. ② orphaned `Navbar.tsx`/`NavbarNav.tsx`/`NavbarMobileMenu.tsx` DELETED (verified zero importers; `NavbarAvatar` kept — still used by DashboardTopBar + DashboardHeader).

### Demo-day hardening · Step 10 — Performance budget (dynamic imports): ✅ COMPLETE (Aug 9, 2026)

**Command-center load path slimmed for the <3s budget.** The map was already lazy: `DisasterMap` is `next/dynamic` with `ssr: false` inside `CommandCenterClient`, but its fallback was a legacy emoji pulse loader — now replaced with a roadmap `SkeletonLoader` block that fills the map viewport (no collapse/reflow while maplibre loads). **New:** `components/dashboard/dynamic/CommandCenterChartsLazy.tsx` — a tiny client island wrapping the Recharts-heavy `CommandCenterCharts` with `ssr: false` + a 3-card skeleton mirroring the chart cards' label/`h-40` anatomy (zero sidebar shift); `app/(dashboard)/command-center/page.tsx` now imports the wrapper instead of the chart directly. **`PredictionChart`** (Recharts) converted inside `CommandCenterClient` to `dynamic(..., { ssr: false })` with an `eoc-panel` + `h-64` skeleton. **Gotcha:** `next/dynamic` + `ssr: false` is FORBIDDEN in Server Components (App Router throws) — the page is RSC (server `cookies()` role resolution + `metadata` export), so all `ssr:false` dynamics live in client modules. No API changes; verified `next build` clean (RSC boundary + dynamic chunks resolve).

## ⚠️ Hard-won gotchas (don't repeat these)

1. Tailwind opacity modifiers on custom colors require `rgb(var(--x-rgb) / <alpha-value>)` in the config — plain `var()` colors silently drop `/40` etc.
2. `bg-primary` / `text-muted` / `border-subtle` / `bg-tertiary` are hand-written classes in globals.css, NOT theme colors — they can't take variants (`hover:bg-tertiary` won't work; use `hover:bg-[var(--bg-tertiary)]`).
3. Conflicting Tailwind utilities are resolved by stylesheet order, not class-list order — passing `className` overrides that conflict with a component's own variant classes silently loses. Don't tint override; add a prop instead.
4. Restart `npm run dev` after editing `tailwind.config.ts` — Next.js doesn't fully hot-reload it.
5. JSX comment syntax: `{/* */}` only, never plain `/* */` inside JSX props.
6. **Abspos elements are clipped by an ancestor's `overflow` when their containing block is inside that ancestor.** An `absolute left-full` tooltip inside an `overflow-x-hidden` scroll container is invisible — use a measured `position: fixed` tooltip instead.
7. **When a fade-out tooltip hides, keep position in a separate state from visibility** — resetting one shared state teleports the element to its `(0,0)` fallback and it fades in the corner.
8. Tailwind `first:`/`last:` variant utilities override the base utility for the first/last child (same specificity, later in the cascade) — standard pattern for dividers.
9. react-hot-toast's per-toast wrappers are absolutely positioned with inline styles — you cannot re-position the stack with CSS; flip the `position` prop via `matchMedia` instead. Hover-pause on the progress bar is visual only (the library's timer keeps running).
10. Prefer roadmap tokens over literal spec colors (`border-blue-500` → `border-accent-primary`) — same look, but re-themes in day-ops mode. Document any deviation in the summary.

## ✅ Validation state (re-verified Aug 9, 2026 — Phases 1, 2, 9, 10 all wired)

**Phase 2 public dashboard · Steps 8–10 landed (same session) — speed-dial, AI teaser, pull-to-refresh:** `components/public/EmergencyDial.tsx` (new) — server-safe 4-square tel: speed-dial (grid-cols-4 aspect-square): Control Room 1070 full red (`bg-severity-red-600` + red glow shadow, hover red-500), Police 100 / Ambulance 108 / Fire 101 as high-contrast white squares with dark `--dl-navy` text + `--dl-blue` icons (spec: dark text on white/gray, red for control room). Each is a plain `<a href="tel:…">` so taps open the native dialer; `aria-label` carries the FULL service name ("District Control Room — 1070" — reviewer nit fixed); icon hover scale + `active:scale-95`, footnote "Taps open your phone's dialer". Mounted after NearbySheltersList; **replaces the old standalone "Call 1070" strip** (deleted — the red control-room square IS that CTA now, so the page has one 1070 action not two; `Megaphone` import dropped). `components/public/AITeaser.tsx` (new) — server-safe card at the very bottom: Bot icon in an orange tile, "Need help preparing?" + AI chip, two suggested-prompt pills ("What should I pack?" / "Is my route safe?") that deep-link to `/public/ai?q=<prompt>` (encodeURIComponent); gotcha-#1-compliant hex-literal alphas throughout (`#F97316`/`#FDBA74`). `app/public/ai/page.tsx` (new) — placeholder AI assistant route (Next 14.2, plain-object `searchParams`), reads `?q=` and shows the picked-up prompt in an orange chip ("Your prompt: …") so the deep link visibly works end-to-end; Back arrow to dashboard, BottomNav, dl-* design language. `components/public/PullToRefresh.tsx` (new, "use client") — wraps the ENTIRE dashboard column content. React onTouchStart/Move/End track a downward drag (only while `window.scrollY <= 0`, single touch, 0.5 resistance, 120px cap, dy<=0 cancels); a SEPARATE non-passive native `touchmove` listener (attached via ref + `{ passive: false }`) does the `preventDefault` — React's root listeners are passive and can't suppress native overscroll/browser PTR; wrapper also has `overscroll-y-contain`. Pull indicator chip rides down with the drag (`translateY(pull-18)`, opacity on pull>0, Loader2 spins only while refreshing), text flips "Pull to refresh" → "Release to refresh" at the 80px trigger; release past trigger pins the spinner at 80px and runs a 1.5s mock refresh (`window.setTimeout`, cleared on unmount), then fires `triggerLightHaptic()` (**reuse of hooks/useHaptics — `navigator.vibrate(15)`, exactly the spec**) and stamps a "Last updated: just now" line at the bottom (`relativeLabel`: <60s → "just now", else HH:MM; `lastUpdatedAt` starts null on BOTH server and first client paint so the footer never hydration-mismatches, set to `new Date()` in a mount effect). Content follows the finger (no transition while dragging) and eases back on release; `aria-live="polite"` on the status text, `motion-reduce:transition-none`. **Reviewer fix applied:** added `onTouchCancel` — a browser touchcancel mid-gesture (system gesture / notification shade / scroll-view takeover) previously left stale `startY`/`dragging` refs that kept preventDefault active and could spuriously arm a refresh; cancel now resets without triggering refresh. Dashboard page: `Megaphone` import removed, module-grid section keeps `flex-1`, EmergencyDial + AITeaser sections added, whole column wrapped in `<PullToRefresh>` (children at tag-indent verified canonical — prettier produces it that way). Validation: tsc clean, lint clean, prettier clean (prettier --write made zero changes), 308/308 tests (no new tests — presentational/gesture only).

**Phase 2 public dashboard · Steps 6–7 landed (same session) — family strip + shelters:** `lib/mock-data/family-contacts.ts` (new) parses localStorage `citizen_family_contacts` (saved during Phase 1 onboarding: `{ name, phone }` array) into status-annotated members — each contact gets a cycling mock status (safe→unknown→danger, deterministic pseudo-random from a phone hash so it's stable per render) with `statusLabel`/`statusTone`; `family-contacts.test.ts` locks it (4 tests). `components/public/FamilyStrip.tsx` (new) — horizontal scroll of circular avatar chips (initials via a 2-letter uppercase helper, no photos in demo), each with a colored status dot (green `severity-green-500` Safe / amber `severity-amber-500` Unknown / pulsing red `severity-red-500`+`animate-pulse` In danger), tap opens a framer-motion bottom-sheet dialog (`role="dialog"` + `aria-modal`, spring sheet + dark backdrop, Esc / backdrop / X close, focus moved into the panel on open + restored to the trigger on close, `MotionConfig reducedMotion="user"`) with two actions — **Nudge for an update** (info toast) and **Broadcast "I am Safe"** (success toast). **Hydration-safe read via `useSyncExternalStore(subscribe, readFamilyContacts, () => [])`** — same pattern as `useSafetyStatus` (the reviewer flagged the initial lazy-`useState` version as a server/client mismatch risk in the SSR dashboard); `subscribe` re-reads on cross-tab `storage` events. Empty state links to `/public/setup/family`. `components/public/NearbySheltersList.tsx` (new) — exactly 3 mock shelters (Patna Central Community Hall 1.2km/15min/45/100 Medical+Food, Riverside High School 2.4km/30min/128/150 Medical, Kankarbagh Stadium Shelter 3.1km/38min/92/250 Food) each with MapPin name + distance, Footprints walk time, beds readout, a severity-tinted occupancy bar (green <50% / amber ≥50% / red ≥80%, `role="img"` + aria-label), Medical/Food facility chips (`Stethoscope`/`UtensilsCrossed`), and a "View All on Map" link to `/public/map` (same convention as the ActionCard PREPARE action). Both mounted in `app/public/dashboard/page.tsx` between the safety stack and the module grid. Review fixes applied: hydration-safe store read, single-icon `NudgeButton` (children carry the icon — the first draft hardcoded a HeartHandshake that doubled up with the passed PhoneCall/Check), plus the missing `next/link` import caught in the post-fix pass. **Tests now 312/312** (308 + 4; the openrouter resolver tests were made hermetic Aug 10 — provider keys blanked in `beforeEach`, see header). Validation: tsc clean, lint clean, prettier clean.

**Phase 2 public dashboard · Steps 4–5 landed (same session) — contextual action + forecast:** `components/public/ActionCard.tsx` (new) — switch/case on `SafetyStatus` telling the citizen what to do next, mounted directly below the hero: SAFE → "View Weather Forecast" button that `scrollIntoView`s to the carousel (`block: nearest`); WATCH → "Check Evacuation Readiness" button with a **collapsible `aria-expanded` UL checklist** (go-bag/docs/shelter route/charged phone); PREPARE → "View Evacuation Routes" link to `/public/map` (future citizen page, same convention as the module grid); EVACUATE → massive pulsing red link "GO TO NEAREST SHELTER" (`/public/shelters`, reuses `.animate-alert-pulse`). Severity states use the re-theming `severity-*` tokens (green/amber/red 300–600) to match SafetyHero — gotcha #10; PREPARE keeps the documented `#F97316`/`#FDBA74` hex literals. `components/public/WeatherCarousel.tsx` (new) — 3-day forecast (Today 120mm warning / Tomorrow 45mm watch / Day 3 dry safe), horizontal snap carousel (`snap-x snap-mandatory snap-center`, `w-[78%] shrink-0` cards, hide-scrollbar via `[scrollbar-width:none] [&::-webkit-scrollbar]:hidden` — the proven SuggestedPrompts pattern, full-bleed `-mx-4 px-4 scroll-px-4` matching HeroKPIs), each card = lucide weather icon + °C + rainfall mm + `SeverityBadge size="sm"` risk chip. **`components/public/SafetyOverview.tsx` (new) replaces the standalone `SafetyStatusCard`** (deleted) — one client island calling `useSafetyStatus()` ONCE and stacking SafetyHero + ActionCard + WeatherCarousel; `app/public/dashboard/page.tsx` mounts `<SafetyOverview />`. Validation: tsc clean, lint clean, prettier clean, 304/304 tests (no new tests this step — presentational only).

**Phase 2 public dashboard · Step 3 landed (same session) — dynamic safety status:** `lib/mock-data/hazard-zones.ts` (new) defines the `SafetyStatus` union (SAFE/WATCH/PREPARE/EVACUATE — now the single source of truth; `SafetyHero` re-exports it), the `HAZARD_ZONES` mock table (6 Bihar districts mirroring the location-setup list, each with a centroid + status; Patna WATCH with village overrides Barh→EVACUATE/Danapur→PREPARE, Muzaffarpur PREPARE w/ Motipur→EVACUATE, Purnia EVACUATE, Gaya/Darbhanga SAFE), `MATCH_RADIUS_KM=60`, pure `resolveSafetyStatus()` (manual = case-insensitive district match + village override; GPS = nearest-centroid via `haversineKm`; unknown district / out-of-radius / no location → SAFE) and `formatSavedAt()` (ISO → "HH:MM:SS IST"). `hooks/useSafetyStatus.ts` (new) reads localStorage `citizen_location` via **`useSyncExternalStore` with `getServerSnapshot: () => null`** — hydration-safe by construction (server and first client paint agree on "Location not set", real value swaps in post-hydration; no mismatch error, no SAFE→EVACUATE flash), `subscribe` re-reads on cross-tab `storage` events. `components/public/SafetyStatusCard.tsx` (new) is the client island that feeds the hook into SafetyHero; `app/public/dashboard/page.tsx` now mounts `<SafetyStatusCard />` instead of hardcoded `status="SAFE"`. Tests: `lib/mock-data/hazard-zones.test.ts` (11 tests — district/village matching, escalation overrides, GPS nearest/out-of-fence, null fallback, haversine bounds, IST math 09:42:17Z → 15:12:17). Also swept three stale unused `ScrollReveal` imports + one unused map-index in legacy landing/`app/sections` files that the previous lint cache had masked (cache cleared → 7 lint errors surfaced, all fixed). **Tests now 304/304.** Validation: tsc clean, lint clean, prettier clean, Tailwind build clean.

**Phase 2 public dashboard · Step 2 landed (same session):** `components/public/SafetyHero.tsx` (new) — massive ~50vh one-glance status card, mounted at the top of `app/public/dashboard/page.tsx` (replacing the small Step 1 status card; `ShieldCheck` import removed from the page). Config-driven `STATUS_THEMES` map keyed by `SafetyStatus = "SAFE" | "WATCH" | "PREPARE" | "EVACUATE"`: SAFE = severity-green tints + "YOU ARE SAFE", WATCH = severity-amber + "STAY VIGILANT", PREPARE = dl-orange (#F97316/#FDBA74 hex literals — gotcha #1: `var()`+alpha modifiers silently drop, so the PREPARE tint uses hex alphas) + "PREPARE TO EVACUATE", EVACUATE = severity-red + reuse of the existing `.animate-alert-pulse` keyframes + "EVACUATE NOW". Each theme: eyebrow label + icon tile (ShieldCheck/Eye/TriangleAlert/Siren, strokeWidth 1.5), massive `text-4xl/5xl font-black` headline, subline, and a "YOUR AREA" footer with `area` (default "Kankarbagh, Patna") + optional mono `updatedAt` readout. Server-safe pure component, `role="status"` + `aria-label="Safety status: …"`. Dashboard passes `status="SAFE" area="Kankarbagh, Patna" updatedAt="09:42:17 IST"`. Validation: tsc clean, lint clean, prettier clean, production Tailwind build clean (all severity-300 shades + `/10`-`/50` alphas generate via the rgb-alpha config).

**Phase 2 public dashboard · Step 1 landed (same session):** `components/public/BottomNav.tsx` (new) + `app/public/dashboard/page.tsx` (rewritten from the Phase 1 placeholder). The citizen app is now a mobile-first phone frame: `max-w-md mx-auto` single column at every breakpoint, sticky citizen BottomNav (`fixed inset-x-0 bottom-0 mx-auto max-w-md`, `pb-[env(safe-area-inset-bottom)]`, `h-[72px]` grid, 4 even tabs — Home/Alerts/Map/SOS — auto-active via `usePathname` + `aria-current`, SOS tab gets the citizen-orange filled treatment as the panic button; uses hex-literal alphas `bg-[#0F2A4F]/95` etc. per gotcha #1 — plain-`var()` arbitrary alphas silently drop). Content column gets `pb-[calc(88px+env(safe-area-inset-bottom))]` so nothing hides behind the fixed nav. Kept: GuestModeBanner, the 4 module links (SOS report/live alerts/shelters/family — future pages, 404 until they land), emergency Call-1070 strip, ambient dl-navy glow. Dropped the old decorative footer (redundant under the fixed nav — intentional). Icons typed `LucideIcon` (codebase convention, not `typeof Home`). Validation: tsc clean, lint clean, prettier clean on both files.

**Aug 9 late-evening tsc drift fixed (nav-verification session):** a post-handoff landing-page workstream (`app/(public)/landing/*`, `components/landing/*` — the DisasterLink AI marketing site, separate from the two-front-door `app/page.tsx`) had broken `tsc` (17 errors) and `next lint` (7 errors). All fixed, validation fully green again: ① `ScrollReveal`/`Eyebrow` in `components/landing/ui/` gained the props the sections were already passing (`animation` fade-up/down/left/right with a `firedRef`-guarded `onViewportEnter` for `onReveal`, and a `text` fallback on Eyebrow — Hero.tsx also had a wrong relative import path, switched to the `@/` alias); ② lucide-react 1.30 removed brand icons — Footer swaps `Instagram`/`Linkedin`/`Twitter` → `AtSign`/`Camera`/`Briefcase`; ③ `VoiceInputButton` timeout ref typed `number | null` (DOM `window.setTimeout`); ④ `ai-rate-limit.ts` Map cleanup loop → `.forEach` (avoids the `downlevelIteration` flag); ⑤ lint: unused imports removed (CommandCenter `ScrollReveal`, EmergencyTimeline `useEffect`, Footer `Link`), `useCountUp` ref now generic `<T extends HTMLElement = HTMLDivElement>` (consumers attach to `<div>`s), `catch {}` without binding in ShareAlertButton, and settings/integrations `icon: any` → `LucideIcon` + dropped a needless `as any` cast. `prettier --write` also normalized the whole landing tree (formatting only — those files predate the prettier convention).

- `npx tsc --noEmit` → clean
- `npx vitest run` → 293/293 pass (32 files; the previously flaky `lib/ai/openrouter.test.ts` env-stub race passed this run too)
- Production Tailwind build (`npx tailwindcss -c tailwind.config.ts -i app/globals.css`) → compiles clean (incl. `z-[9999]`, `translate-y-[25vh]`, `border-t-accent-primary`, `bottom-[84px]`, severity/accent opacity modifiers)
- Prettier (`npx prettier --check`) → clean on all touched files
- Phase 9/10 + Phase 11·5 browser checks ALL PASSED (Aug 9 evening — `node scripts/check-phase9-10.mjs`, Playwright with direct guest-cookie auth), now **16/16**: guest /command-center render + KPI count-up (mid-flight value observed, settles exactly 48,210) + live IST clock ticking; offline banner (`context.setOffline(true)` → amber banner → Retry toast "Still offline" → dismiss on reconnect); one-handed double-tap at 900px (slide down + Restore chip + negative control: tab double-tap does NOT toggle); demo simulation with `demo_sim_active` (people-at-risk 43,653 → 48,217 + feed 10 → 20 rows); DemoController (hidden without `?demo=1`, shown with it, scenario pill fires roadmap toast); **ShortcutModal** (`?` opens with 18 `<kbd>` chips + hidden before press ✓, Esc closes ✓, `⌘/` navigates to /ai-planner ✓). **16/16 PASS, zero real console errors** (alerts-500 = Supabase DB unreachable in dev + offline-chunk/websocket noise are filtered as known; WhatIfSimulator/maplibre fetch failures only occur while deliberately offline and are handled gracefully).
- ~~Known overlay (pre-existing, Phase 22-era)~~ **FIXED Aug 9** — the bottom-right corner's fixed elements no longer cover the BottomNav: `SimulationToggle` moved to the bottom-LEFT (`bottom-[calc(72px+env(safe-area-inset-bottom)+12px)] left-4 z-50 md:bottom-4 md:left-4`) and `EmergencyContactCard` is `hidden md:flex` (phones use the BottomNav's SOS EmergencyFAB). The real overlay culprit was the emergency card (228px wide, covered the Alerts/More tabs at ~600px), not the toggle — both are resolved. Full `check-phase9-10.mjs` re-ran **16/16 PASS, zero real console errors** on a FRESH dev server (the old long-running dev server had corrupted its chunk manifest after the file deletions — the page rendered but never hydrated; restart fixed it).
- Server-render verified via curl: `/styleguide` HTTP 200; `/command-center` (guest cookie) HTTP 200 with all sidebar markers in the HTML
- Note: live flood-fetch API fails gracefully with fallback ("upstream down") — expected in dev without keys
- Browser-verified (interactive, live dev server): styleguide composed-nav demo — collapse to 64px rail hides labels ✓, hover tooltip on rail icon ✓, expand restores ✓; **role-filter demo** — district_admin shows 9 items (no Settings) ✓, field_responder hides Shelters/Resources/Satellite ✓, super_admin shows all 10 incl. Settings ✓; **accordion demo (Step 4)** — Settings row shows chevron ✓, click expands nested Profile/Notifications/Map/AI with chevron rotated ✓, click again collapses ✓, collapsing the rail then clicking the Admin parent auto-expands the sidebar AND opens its sub-menu ✓; wired `/command-center` (guest) — fixed sidebar renders with config-driven nav ✓, map canvas sits right of the sidebar ✓, collapse shifts the map as the content margin animates ✓, zero console errors ✓; **QuickActions (Step 5)** — panel renders above the collapse footer when expanded ✓, disappears at the 64px rail ✓, both buttons fire the roadmap toast card ✓; **hotkeys (Step 6)** — ⌘1–⌘6 badges render on the first row per href ✓, global mod+key navigation + typing-in-input guard implemented (live browser check pending); **mobile drawer + persistence (Step 7)** — desktop collapse persists to `drip:sidebar-collapsed` and reloads on mount ✓, collapse chevron desktop-only ✓, drawer off-screen on mobile ✓, hamburger opens overlay with dark backdrop ✓, backdrop/click or Escape closes with scroll-lock ✓ (live browser check at a <1024px viewport pending)
- Server-render verified via curl: `/styleguide` HTTP 200; `/command-center` (guest cookie) HTTP 200 with all sidebar markers in the HTML
- Note: live flood-fetch API fails gracefully with fallback ("upstream down") — expected in dev without keys

## 🔜 Next up (where we stopped)

**UI/UX Phases 1, 2, 9, 10 are DONE and WIRED (Aug 9, 2026).** Design system (Phase 1), web sidebar (Phase 2, Steps 1–10), mobile/field polish (Phase 9: biometric prompt wired into login, offline banner mounted in the dashboard layout, one-handed reachability mode), and demo-day polish (Phase 10: CountUpNumber with `from` prop, LiveClock in the header, useDemoSimulation + consumers wired, DemoController mounted at root behind `?demo=1`).

**Phase 2 public dashboard (citizen safety) is IN PROGRESS — Steps 1–10 landed this session:** Step 1 mobile-first phone frame + BottomNav (Home/Alerts/Map/SOS) → Step 2 SafetyHero one-glance status card → Step 3 geo-fenced dynamic status (`useSafetyStatus` + `hazard-zones.ts`) → Step 4 ActionCard (contextual per status) → Step 5 WeatherCarousel (3-day snap forecast) → Step 6 FamilyStrip (avatar strip + nudge/broadcast modal) → Step 7 NearbySheltersList (3 mock shelters + occupancy bars) → Step 8 EmergencyDial (4-square tel: speed-dial, red control room) → Step 9 AITeaser + `/public/ai` placeholder (pills deep-link with `?q=`) → Step 10 PullToRefresh (touch PTR, 1.5s mock refresh, haptic, "Last updated" stamp). All mounted on `/public/dashboard`; 308/308 tests. **Next steps per the roadmap:** ~~live alerts feed (`/public/alerts`)~~ — **DONE Aug 10, Phase 3 Steps 1–2** (see header note; remaining Phase 3 steps: detail view, read-state persistence, notifications) · citizen map (`/public/map`), SOS report (`/public/report` — reuse the existing spam-checked report action), family circle page (`/public/family`), and the shelter detail flow. Each component is self-contained and mock-data-driven (`lib/mock-data/`), so they'll snap to real Supabase/API data later. The two-front-door landing (`app/page.tsx`), Phase 1 auth/onboarding flow, and both dashboards remain from earlier phases.

**Still open (confirmed in the code):** ① **wire the REAL user role** — DONE Aug 9: `app/(dashboard)/layout.tsx` now passes `userRole` (guest → `district_admin` demo default, real user → validated `profile.role`) through `DashboardShell` → `DashboardSidebar` (reviewed + type-checked; `profile.role` is validated against `OPERATIONAL_ROLES` before the cast). ② **accordion `children` into the nav config** — DONE Aug 9: Settings gained children (Profile/Notifications/Map/AI) in `lib/config/navigation.ts` via `NavRoute.children?: SidebarSubRoute[]`; `DashboardSidebar` passes them through. Verified: hotkeys unaffected (mod+1..6 are hardcoded to non-Settings routes) and the collapsed-rail accordion works (parent click calls `expand()` first, sub-menu renders only when expanded). ③ **interactive browser checks** — DONE Aug 9 evening (13/13 PASS, see Validation section + `scripts/check-phase9-10.mjs`; two demo-surface fixes landed: KPICards subscribes to the demo-sim people event, and LiveClock now also mounts in DashboardTopBar). ④ count up the legacy `KPICards.tsx` values — DONE Aug 9 (values wrapped in `CountUpNumber`); a `demo_sim_active` toggle button for judges is still optional.

Optional follow-ups: remove the now-orphaned `Navbar.tsx`/`NavbarNav.tsx`/`NavbarMobileMenu.tsx` (no layout imports them after the drawer hamburger replacement) and migrate the remaining ~70 legacy `toast.success("msg")` call sites to `useToast()` for the accent-border treatment everywhere.

**Known open decisions:**

- **Dark mode:** RESOLVED — dark LOCKED for demo day (`forcedTheme="dark"` in ThemeProvider; ThemeToggle removed from landing/Navbar/DashboardTopBar and deleted). Day-ops palette stays in globals.css but is unreachable until `forcedTheme` is dropped post-hackathon.
- **Toast migration:** legacy `toast.success("msg")` call sites (~70) get the ToastViewport re-skin but not the full custom card; migrating them to `useToast()` for the accent-border treatment everywhere is an optional follow-up.

---

## 📋 NEXT-CHAT PROMPT (paste this to resume)

> DRIP project context handoff — read `docs/CONTEXT_HANDOFF.md` in `disaster-response-platform/` for the full history, then verify the current state of the Phase 9/10 components and their wiring: `components/auth/BiometricPrompt.tsx` (+ `app/(auth)/login/LoginForm.tsx`), `components/ui/OfflineBanner.tsx` + `hooks/useOnlineStatus.ts` (+ `app/(dashboard)/layout.tsx`), `BottomNav.tsx`/`DashboardShell.tsx` (one-handed mode), `components/ui/CountUpNumber.tsx` + `components/dashboard/HeroKPIs.tsx`, `components/dashboard/LiveClock.tsx` (+ `DashboardHeader.tsx`), `hooks/useDemoSimulation.ts` (+ `HeroKPIs.tsx`/`LiveActivityFeed.tsx`/`DashboardShell.tsx`), and `components/demo/DemoController.tsx` (+ `app/layout.tsx`).
>
> Where we are: Phase 1 (design system) is COMPLETE — roadmap tokens in globals.css + tailwind.config.ts (with the rgb-alpha opacity fix), SeverityBadge, StatusDot, StatCard, DataRow, Panel, IconButton, BackButton, SkeletonLoader (+SkeletonCard/SkeletonRow), Toast/useToast/ToastViewport (react-hot-toast wrap), the /styleguide internal Storybook, Inter/JetBrains Mono fonts, and the next-themes dark-LOCKED provider (`forcedTheme="dark"`; ThemeToggle removed from landing/Navbar/DashboardTopBar and deleted — see Demo-day hardening · Step 6). Phase 2 (web sidebar) Steps 1–10 are DONE and WIRED — collapsible Sidebar shell (260⇄64px, fixed/inline, controlled+uncontrolled, localStorage persistence, mobile drawer below md), SidebarHeader, sidebar-context (collapsed + expand()), SidebarSection, SidebarNavItem (auto-active, badge pill, fixed-position tooltips, accordion children), lib/config/navigation.ts (10 routes + allowedRoles; role matrix field_responder ⊂ district_admin ⊂ super_admin — real role NOW WIRED: layout passes guest→district_admin, real user→validated profile.role through DashboardShell → DashboardSidebar; Settings has accordion children Profile/Notifications/Map/AI), DashboardSidebar, DashboardShell + DashboardTopBar, QuickActions, global hotkeys mod+1..6, live Active Alerts badge. Phase 9 (mobile & field polish) is DONE and WIRED — BiometricPrompt shows after first real email login (skipped when `drip_biometric_enabled_v1` is stored; Google/guest skip it), OfflineBanner + useOnlineStatus mounted in the dashboard layout (amber drop-down, mock last-synced, Retry Connection), and one-handed Reachability mode (double-tap the BottomNav gutter → content slides down 25vh; Restore chip + desktop auto-exit; the swipe-cancel must live in onSwiped*, NOT onTouchStartOrOnMouseDown). Phase 10 (demo-day polish) is DONE and WIRED — CountUpNumber (rAF, ease-out, en-IN commas, reduced-motion; optional `from` prop for live prev→new anims) wrapping HeroKPIs' three numeric cards AND the legacy KPICards values, LiveClock (IST, muted seconds, role=timer) in DashboardHeader AND DashboardTopBar (new `timeClassName` prop; `hidden lg:flex` — ticks on every dashboard page incl. the guest command center), useDemoSimulation (8s interval gated on localStorage `demo_sim_active`; dispatches `drip:demo-sim:people-at-risk` + `drip:demo-sim:activity`; mounted in DashboardShell; HeroKPIs + KPICards + LiveActivityFeed subscribed — the guest /command-center People-at-Risk card drifts too), and DemoController (bottom bar at `z-[9999]` when the URL has `?demo=1`; SCENARIOS table → roadmap toasts; mounted in root layout). All validation green (Aug 10, 2026): tsc clean, Tailwind build clean, Prettier clean, 312/312 tests (openrouter.test.ts made hermetic — real provider keys are blanked in beforeEach so `.env`/shell keys can't leak; previously 4 failures on machines with keys set). **Interactive browser checks ALL PASSED (16/16, zero real console errors) via `node scripts/check-phase9-10.mjs`** (offline banner, one-handed double-tap at 900px + Restore chip + tab negative control, count-up + clock tick, demo simulation injection, `?demo=1` controller, ShortcutModal: `?` opens kbd grid ✓ Esc closes ✓ `⌘/` → AI Planner ✓ + closes on route change ✓). Phase 11 · Step 5 ShortcutModal is DONE and WIRED — `components/ui/ShortcutModal.tsx` mounted in the root layout: `?` (Shift+/) opens a roadmap-token dark modal listing the real hotkeys as kbd chips, Esc/backdrop/X close, ⌘/ alias wired (⌘1–6 from Phase 2 shown as-is; ⌘2 = Alerts, NOT Map — deliberate spec deviation, see handoff). Known overlay: the root layout's fixed SimulationToggle (bottom-4 right-4, z-50) covers the BottomNav's far-right gutter at short viewports — double-tap the left/center gutter there.
>
> Next up: if the phase spec intends ⌘2 = Map, re-map `NAV_SHORTCUTS`/`NAV_SHORTCUT_LABELS` in DashboardSidebar (currently ⌘2 = Alerts) and the ShortcutModal grid; optionally add a demo_sim_active toggle button for judges (`setDemoSimActive()` already exists in hooks/useDemoSimulation.ts; the hook reacts to the same-tab `drip:demo-sim:changed` event instantly). AI providers note: all four keys are set and live-probed OK except the primary OpenRouter key (402, no credits — resolver skips it); if the chat shows "all providers probed", restart `npm run dev` first (stale env at boot) before touching the keys. Optional cleanup: remove orphaned Navbar.tsx/NavbarNav.tsx/NavbarMobileMenu.tsx and migrate the ~70 legacy toast call sites to useToast().
>
> Hard rules: follow the gotchas in the handoff (hand-written classes like bg-tertiary can't take variants; roadmap tokens over literal colors; JSX comments only as {/* */}; restart dev after tailwind.config.ts changes). Keep the hybrid approach — do NOT strip legacy EOC tokens. Validate with tsc + production Tailwind build + Prettier after each step, and update this file's "where we stopped" when a step completes.
